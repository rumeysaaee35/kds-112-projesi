<!DOCTYPE html>
a<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>112 AKILLI KDS | Karar Destek Sistemi</title>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <script src="https://leaflet.github.io/Leaflet.heat/dist/leaflet-heat.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        :root { --sidebar-bg: #1a1a2e; --active-blue: #1d8cf8; --main-red: #fd5d93; --card-bg: #27293d; --text-gray: #9a9a9a; --purple-btn: #ba54f5; --green-accent: #00f2c3; --dark-input: #1e1e2f; }
        body { margin: 0; display: flex; font-family: 'Poppins', sans-serif; background: #1e1e2f; color: white; height: 100vh; overflow: hidden; }

        /* --- SIDEBAR --- */
        .sidebar { width: 230px; background: var(--sidebar-bg); display: flex; flex-direction: column; padding: 25px; box-shadow: 5px 0 15px rgba(0,0,0,0.3); z-index: 1000; position: fixed; height: 100vh; }
        .logo { font-size: 18px; font-weight: bold; color: var(--green-accent); margin-bottom: 40px; display: flex; align-items: center; gap: 12px; }
        .nav-link { color: rgba(255,255,255,0.7); padding: 15px; text-decoration: none; border-radius: 10px; margin-bottom: 10px; display: flex; align-items: center; gap: 12px; transition: 0.3s; cursor: pointer; }
        .nav-link:hover { background: rgba(255,255,255,0.1); color: white; }
        .nav-link.active { background: var(--active-blue); color: white; box-shadow: 0 4px 15px rgba(29, 140, 248, 0.4); }

        /* --- ANA İÇERİK --- */
        .content-area { flex: 1; margin-left: 260px; overflow-y: auto; padding: 30px; scroll-behavior: smooth; }
        .page-section { display: none; animation: fadeIn 0.3s ease; }
        .page-section.active { display: block; }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

        .card { background: var(--card-bg); border-radius: 15px; padding: 25px; margin-bottom: 25px; box-shadow: 0 10px 30px rgba(0,0,0,0.2); }
        .kpi-row { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; margin-bottom: 30px; }
        .kpi-card { background: var(--card-bg); padding: 20px; border-radius: 15px; text-align: center; border-bottom: 4px solid var(--active-blue); cursor: pointer; transition: 0.3s; position: relative; overflow: hidden; }
        .kpi-card:hover { transform: translateY(-5px); border-bottom-color: var(--purple-btn); }
        .kpi-card h5 { margin: 0; color: var(--text-gray); font-size: 11px; text-transform: uppercase; }
        .kpi-card .val { font-size: 20px; font-weight: bold; margin-top: 10px; }

        /* --- YÖNETİCİ PANELİ --- */
        .admin-container { background: var(--card-bg); border-radius: 20px; padding: 45px; max-width: 850px; margin: auto; border: 1px solid rgba(255,255,255,0.05); }
        .admin-label { display: block; color: #fff; font-size: 14px; margin-bottom: 12px; font-weight: 500; }
        .admin-input, .admin-select { width: 100%; padding: 14px; background: var(--dark-input); border: 1px solid rgba(255,255,255,0.1); color: white; border-radius: 10px; margin-bottom: 30px; outline: none; transition: 0.3s; }
        .btn-update { width: 100%; padding: 16px; background: var(--purple-btn); border: none; color: white; border-radius: 40px; font-weight: bold; cursor: pointer; font-size: 15px; transition: 0.3s; box-shadow: 0 5px 15px rgba(186, 84, 245, 0.3); }
        input[type=range] { width: 100%; accent-color: var(--purple-btn); margin: 20px 0; }

        /* --- SAĞDAN KAYAN PANEL --- */
        .side-panel { position: fixed; right: -450px; top: 0; width: 400px; height: 100%; background: #16213e; transition: 0.5s; z-index: 2000; padding: 25px; border-left: 3px solid var(--purple-btn); box-shadow: -10px 0 30px rgba(0,0,0,0.5); }
        .side-panel.active { right: 0; }
        .prescriptive-text { background: rgba(0, 242, 195, 0.1); border-left: 4px solid var(--green-accent); padding: 15px; color: var(--green-accent); font-style: italic; font-size: 13px; border-radius: 5px; }

        table { width: 100%; border-collapse: collapse; }
        th { text-align: left; padding: 15px; color: var(--active-blue); border-bottom: 1px solid rgba(255,255,255,0.1); font-size: 12px; }
        td { padding: 15px; border-bottom: 1px solid rgba(255,255,255,0.05); font-size: 13px; }
        .badge { padding: 4px 12px; border-radius: 20px; font-size: 10px; font-weight: bold; }
        .bg-red { background: rgba(253, 93, 147, 0.1); color: #fd5d93; border: 1px solid #fd5d93; }
        .bg-green { background: rgba(0, 242, 195, 0.1); color: var(--green-accent); border: 1px solid var(--green-accent); }

        .ilce-item { background: rgba(255, 255, 255, 0.03); border-radius: 10px; padding: 12px; margin-bottom: 12px; border: 1px solid rgba(255, 255, 255, 0.05); transition: 0.3s; }
        .ilce-info { display: flex; justify-content: space-between; margin-bottom: 8px; font-size: 13px; }
        .ilce-name { font-weight: bold; color: #e0e0e0; }
        .ilce-perc { color: #fd5d93; font-weight: bold; }
        .progress-container { height: 6px; background: rgba(255, 255, 255, 0.1); border-radius: 10px; overflow: hidden; }
        .progress-bar { height: 100%; background: linear-gradient(90deg, #fd5d93, #ec250d); border-radius: 10px; transition: width 1s ease-in-out; }

    </style>
</head>
<body>

<div class="sidebar">
    <div class="logo"><i class="fas fa-hand-holding-medical"></i> 112 ACİL KOMUTA SİSTEMİ</div>
    <div class="nav-link active" onclick="switchPage('dashboard', this)"><i class="fas fa-chart-pie"></i> Dashboard</div>
    <div class="nav-link" onclick="switchPage('geo', this)"><i class="fas fa-map-marked-alt"></i> Coğrafi Analiz</div>
    <div class="nav-link" onclick="switchPage('data', this)"><i class="fas fa-database"></i> Veri Merkezi</div>
    <div class="nav-link" onclick="switchPage('admin', this)"><i class="fas fa-user-shield"></i> Kontrol Paneli</div>
    
    <div class="card" style="background: rgba(29, 140, 248, 0.05); margin-top: auto; padding: 15px; border: 1px solid rgba(29,140,248,0.2);">
    <h5 style="color: #1d8cf8; font-size: 11px; margin-top: 0; letter-spacing: 1px;">KDS SİSTEM SAĞLIĞI</h5>
    <div style="text-align: center; margin: 10px 0;">
        <div id="kdsIndexScore" style="font-size: 28px; font-weight: bold; color: #00f2c3; text-shadow: 0 0 10px rgba(0,242,195,0.3);">%84</div>
        <div style="font-size: 9px; color: var(--text-gray); text-transform: uppercase;">Genel Verimlilik Endeksi</div>
    </div>
    <div style="font-size: 11px; line-height: 2;">
        Kritik Bölge: <b id="sideCritical" style="color: #fd5d93; float: right;">0</b><br>
        Aktif Risk: <b id="sideRiskStatus" style="float: right; color:#ff8d72;">DÜŞÜK</b>
    </div>
    <div class="progress-container" style="height: 4px; margin-top: 10px; background: rgba(255,255,255,0.05);">
        <div id="kdsIndexBar" class="progress-bar" style="width: 84%; background: linear-gradient(90deg, #00f2c3, #1d8cf8);"></div>
    </div>
</div>
    <a href="/logout" class="nav-link" style="color: var(--main-red);"><i class="fas fa-sign-out-alt"></i> Çıkış Yap</a>
</div>

<div class="content-area">
    <section id="page-dashboard" class="page-section active">
        <div class="kpi-row">
            <div class="kpi-card"><h5>Toplam Vaka</h5><div class="val">5,000</div></div>
            <div class="kpi-card" style="border-color:var(--main-red);"><h5>Kritik Bölgeler</h5><div class="val" id="dashCritical" style="color:var(--main-red)">0</div></div>
            <div class="kpi-card"><h5>Ort. Ulaşım Süresi</h5><div class="val">12.0 dk</div></div>
            <div class="kpi-card" style="border-color:var(--green-accent);"><h5>Aktif İstasyon</h5><div class="val">50</div></div>
        </div>

        <div class="card" style="border-left: 5px solid var(--purple-btn); background: rgba(186, 84, 245, 0.05);">
            <h4 style="margin-top:0;"><i class="fas fa-search-location"></i>  Filtreleme ve Senaryo Analizi</h4>
            <div style="display: grid; grid-template-columns: repeat(4, 1fr); gap: 15px;">
                <div>
                    <label class="admin-label">Analiz Bölgesi</label>
                    <select id="analysisDistrict" class="admin-select" onchange="runSmartAnalysis()">
                        <option value="all">Tüm İzmir</option>
                        <option value="Bornova">Bornova</option>
                        <option value="Buca">Buca</option>
                        <option value="Konak">Konak</option>
                    </select>
                </div>
                <div>
                    <label class="admin-label">Zaman Dilimi</label>
                    <select id="analysisTime" class="admin-select" onchange="runSmartAnalysis()">
                        <option value="peak">Pik Saatler (08:00-10:00)</option>
                        <option value="night">Gece (00:00-06:00)</option>
                        <option value="all">Tüm Gün</option>
                    </select>
                </div>
                <div>
                    <label class="admin-label">Vaka Tipi Filtresi</label>
                    <select id="analysisType" class="admin-select" onchange="runSmartAnalysis()">
                        <option value="all">Tüm Vakalar</option>
                        <option value="Trafik Kazası">Trafik Kazası</option>
                        <option value="Kalp Krizi">Kalp Krizi</option>
                    </select>
                </div>
                <div style="display: flex; align-items: flex-end;">
                    <button class="btn-update" onclick="runSmartAnalysis()" style="margin-bottom: 25px; width: 100%;">
                        <i class="fas fa-sync"></i> Analizi Çalıştır
                    </button>
                </div>
            </div>
        </div>

        <div id="smartAnalysisResult" class="card" style="display:none; border: 1px dashed var(--green-accent);">
            <div style="display: grid; grid-template-columns: 1fr 2fr; gap: 20px;">
                <div id="analysisSummary" class="prescriptive-text"></div>
                <div style="height: 250px;"><canvas id="dynamicComparisonChart"></canvas></div>
            </div>
        </div>

        <div class="card" id="simulationArea" style="display: none; border: 1px solid var(--purple-btn); background: rgba(186, 84, 245, 0.05); animation: slideDown 0.5s ease;">
            <h3 style="color: var(--purple-btn); margin-top: 0; display: flex; align-items: center; gap: 10px;">
                <i class="fas fa-microchip"></i> KDS Optimizasyon Simülasyonu
            </h3>
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 30px; text-align: center; margin-top: 20px;">
                <div style="padding: 20px; background: rgba(253, 93, 147, 0.05); border-radius: 15px;">
                    <h5 style="color: var(--text-gray); margin-bottom: 10px;">MEVCUT DURUM</h5>
                    <div id="currentTimeVal" style="font-size: 36px; font-weight: bold; color: var(--main-red); margin-bottom: 15px;">12.0 dk</div>
                    <canvas id="currentStatusChart" height="120"></canvas>
                </div>
                <div style="padding: 20px; background: rgba(0, 242, 195, 0.05); border-radius: 15px; border-left: 1px dashed rgba(255,255,255,0.1);">
                    <h5 style="color: var(--text-gray); margin-bottom: 10px;">SİMÜLASYON HEDEFİ</h5>
                    <div id="simTimeVal" style="font-size: 36px; font-weight: bold; color: var(--green-accent); margin-bottom: 15px;">9.4 dk</div>
                    <canvas id="simResultChart" height="120"></canvas>
                </div>
            </div>
            <div class="prescriptive-text" style="margin-top: 20px; text-align: center; font-size: 14px; color: var(--green-accent);">
                <i class="fas fa-check-double"></i> <b>KDS Sonucu:</b> Yeni konumlandırma stratejisi ile kritik vakalara müdahale süresinde belirgin iyileşme saptandı.
            </div>
        </div>

        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-top: 25px;">
            <div class="card">
                <h4 style="font-size: 14px;"><i class="fas fa-lira-sign"></i> Maliyet / Verimlilik Analizi</h4>
                <div style="height: 200px;"><canvas id="costEfficiencyChart"></canvas></div>
                <div id="costInsight" class="prescriptive-text" style="margin-top:10px; font-size:11px;"></div>
            </div>
            <div class="card">
                <h4 style="font-size: 14px;"><i class="fas fa-chart-bubble"></i> Ekip Performans Matrisi </h4>
                <div style="height: 200px;"><canvas id="teamBubbleChart"></canvas></div>
            </div>
        </div>

        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 25px; margin-top: 25px;">
            <div class="card">
                <h4><i class="fas fa-users-cog"></i> Ekip Hazırlık ve Yorgunluk</h4>
                <div id="ekipListesi" style="max-height: 300px; overflow-y: auto;"></div>
            </div>
            <div class="card" style="height: 400px;"> 
                <h4><i class="fas fa-hospital-user"></i> Kritik Hastane Kapasiteleri</h4>
                <div style="position: relative; height: 300px;">
                    <canvas id="hastaneDolulukChart"></canvas>
                </div>
            </div>
        </div>

        <div style="display: grid; grid-template-columns: repeat(2, 1fr); gap: 25px; margin-top: 25px;">
            <div class="card"><h4>Pandemi Etki Analizi (dk)</h4><canvas id="pandemiChart"></canvas></div>
            <div class="card"><h4>Hava Durumu / Gecikme</h4><canvas id="weatherChart"></canvas></div>
            <div class="card"><h4>Vaka Tipi Dağılımı</h4><canvas id="typeChart"></canvas></div>
            <div class="card"><h4>Hastane Türü Erişim Analizi</h4><canvas id="hospitalChart"></canvas></div>
        </div>

        
    </section>

    <section id="page-geo" class="page-section">
        <div style="display: grid; grid-template-columns: 2.2fr 1fr; gap: 20px;">
            <div>
                <div class="card" style="padding:0; overflow:hidden;">
                    <div id="mapGeo" style="height: 550px;"></div>
                </div>
                <div id="aiSuggestionBox" class="card" style="margin-top:20px; border-left:5px solid var(--green-accent); display:none;">
                    <h4 style="color:var(--green-accent); margin:0 0 10px 0;">
                        <i class="fas fa-robot"></i> KDS Prescriptive Öneri
                    </h4>
                    <p id="aiSuggestionText" style="font-size:13px; color:white; line-height:1.6; margin:0;"></p>
                </div>
            </div>
            
</div>
    </section>

    <section id="page-data" class="page-section">
        <div class="kpi-row">
            <div class="kpi-card"><h5>Kayıt Sayısı</h5><div class="val">5000</div></div>
            <div class="kpi-card" style="border-color:#ba54f5;"><h5>En Yoğun İstasyon</h5><div class="val" style="color:#ba54f5;">Buca 1 Nolu</div></div>
            <div class="kpi-card" style="border-color:var(--green-accent);"><h5>Hastane Doluluk</h5><div class="val" style="color:var(--green-accent);">%68</div></div>
            <div class="kpi-card" style="border-color:var(--main-red);"><h5>En Yüksek Süre</h5><div class="val" style="color:var(--main-red)">34 dk</div></div>
        </div>

        <div class="card" style="margin-top: 25px;">
            <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:20px;">
                <h4><i class="fas fa-history"></i> Yıllık Gecikme Trendi ve Kritik 3 İlçe</h4>
                <select id="yearSelect" class="admin-select" style="width:180px; margin-bottom:0;" onchange="updateYearlyAnalysis()">
                    <option value="2020">2020 Verileri</option>
                    <option value="2021">2021 Verileri</option>
                    <option value="2022">2022 Verileri</option>
                    <option value="2023">2023 Verileri</option>
                    <option value="2024">2024 Verileri</option>
                    <option value="2025" selected>2025 Öngörüsü</option>
                </select>
            </div>
            <div style="display: grid; grid-template-columns: 1.2fr 1fr; gap: 30px; align-items: center;">
                <div style="height: 380px;"><canvas id="top3DistrictsChart"></canvas></div>
                <div id="yearInsight" class="prescriptive-text" style="height: fit-content; font-size: 14px; line-height: 1.8;"></div>
            </div>
        </div>

        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-top: 25px;">
            <div class="card"><h5><i class="fas fa-clock"></i> Vardiya Gecikme Yükü</h5><div style="height: 200px;"><canvas id="shiftDelayChart"></canvas></div></div>
            <div class="card"><h5><i class="fas fa-gas-pump"></i> Operasyonel Verimlilik</h5><div style="height: 200px;"><canvas id="fuelEfficiencyChart"></canvas></div></div>
            <div class="card"><h5><i class="fas fa-brain"></i> Personel Stres</h5><div style="height: 200px;"><canvas id="staffStressChart"></canvas></div></div>
            <div class="card"><h5><i class="fas fa-route"></i> Altyapı Sorunları</h5><div style="height: 200px;"><canvas id="roadConditionChart"></canvas></div></div>
        </div>

       
    </section>

    <section id="page-admin" class="page-section">
        <div class="admin-container" style="max-width: 800px; margin: 0 auto;">
            <h2 style="margin-top:0;"><i class="fas fa-cogs"></i> Sistem Yapılandırması</h2>
            <div class="card" style="background: rgba(0, 242, 195, 0.05); border: 1px solid rgba(0, 242, 195, 0.2); padding: 15px; font-size: 13px; margin-bottom: 30px; color: var(--green-accent);">
                <i class="fas fa-info-circle"></i> Duyarlılık Analizi: Eşik değerini düşürmek, KDS'nin riskli bölgeleri daha hassas saptamasını sağlar.
            </div>
            <label class="admin-label">Kritik Gecikme Eşiği (Dakika)</label>
            <input type="number" value="10" id="thresholdInput" class="admin-input">
            <label class="admin-label">Konum Optimizasyon Algoritması</label>
            <select class="admin-select">
                <option>Ağırlık Merkezi (Center of Gravity)</option>
                <option>K-Means Clustering Analysis</option>
            </select>
            <label class="admin-label">Tahmini İyileşme Katsayısı (%)</label>
            <input type="range" min="5" max="50" value="22" id="improvementRange" style="width: 100%;">
            <button class="btn-update" onclick="handleUpdate()" style="margin-top: 20px;">
                <i class="fas fa-sync-alt"></i> Parametreleri Güncelle ve Hesapla
            </button>
        </div>
    </section>
</div>

<div id="analizPanel" class="side-panel">
    <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:20px;">
        <h3>Vaka Raporu</h3><i class="fas fa-times" onclick="closeAnalizPanel()" style="cursor:pointer; font-size:24px;"></i>
    </div>
    <canvas id="analizRadarChart"></canvas>
    <p id="analizOneri" class="prescriptive-text"></p>
</div>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
<script src="/js/geo-logic.js"></script>
<script src="/js/kds-logic.js"></script>
<script src="/js/app-main.js"></script>
</body>
</html>